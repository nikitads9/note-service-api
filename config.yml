grpc:
  host: "localhost"
  port: ":50051"

http:
  host: "localhost"
  port: ":8000"

database:
  host: "localhost"
  port: "5432"
  user: "postgres"
  database: "notes_db"
  ssl: "disable"
  max_opened_connections: "10"